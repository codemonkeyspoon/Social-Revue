<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}} Likeability {{up_score}} Bummer {{down_score}}
    </div>
    <div class="text">
      {{text}}
    </div>
  </section>
  {{/each}}
</div>